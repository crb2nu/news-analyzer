apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: news-analyzer
resources:
  - builder-configmap.yaml
  - kaniko-cache-pvc.yaml
  - kaniko-nightly-cronjobs.yaml
  # kaniko-build-job.yaml can be applied on-demand
