apiVersion: v1
kind: Secret
metadata:
  name: news-analyzer-secrets
  namespace: news-analyzer
  labels:
    app: news-analyzer
type: Opaque
stringData:
  # E-edition credentials
  EEDITION_USER: "cody.r.blevins@gmail.com"
  EEDITION_PASS: "90FcWdIeSWV"
  
  # SmartProxy credentials
  SMARTPROXY_USERNAME: "spua66m4sy"
  SMARTPROXY_PASSWORD: "7h4nhZm69jvME~mslX"
  
  # Database connection - must match postgres-secret in postgres-deployment.yaml
  # NOTE: If you change the password in postgres-secret, update it here too
  DATABASE_URL: "postgresql://news_analyzer:changeme-strong-password-here@postgres-service:5432/news_analyzer"
  
  # MinIO credentials - must match minio-secret in minio-statefulset.yaml
  # NOTE: If you change these in minio-secret, update them here too
  MINIO_ACCESS_KEY: "news-analyzer"
  MINIO_SECRET_KEY: "changeme-strong-secret-key"
  
  # OpenAI API key
  OPENAI_API_KEY: "sk-litellm-local"
  
  # Ntfy credentials (empty for now, will set up later)
  NTFY_TOKEN: ""
  
  # Slack webhook URL (empty as not configured)
  SLACK_WEBHOOK_URL: ""